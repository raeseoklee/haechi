name: Publish haechi-dashboard

# Triggered by publishing a GitHub Release whose tag is `dashboard-v<semver>`.
# Core releases use `v<semver>`; the `if:` guard keeps the workflows from cross-firing.
on:
  release:
    types: [published]

permissions:
  contents: write       # upload signed artifacts to the GitHub release
  id-token: write       # OIDC for npm trusted publishing + sigstore attestation
  attestations: write   # sigstore build-provenance attestation of the tarball

jobs:
  publish:
    runs-on: ubuntu-latest
    if: startsWith(github.event.release.tag_name, 'dashboard-v')
    steps:
      - name: Validate satellite release tag
        run: |
          tag='${{ github.event.release.tag_name }}'
          if [[ ! "$tag" =~ ^dashboard-v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::tag '$tag' is not a dashboard-v<semver> tag; refusing to publish"
            exit 1
          fi
      # actions/checkout v6
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      # actions/setup-node v6.4.0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: "22.x"
          package-manager-cache: false
      - run: npm install -g npm@^11.5.1
      - run: npm ci
      - name: Verify tag matches package version
        run: |
          tag='${{ github.event.release.tag_name }}'
          tag_version="${tag#dashboard-v}"
          pkg_version=$(node -p "require('./satellites/dashboard/package.json').version")
          if [[ "$tag_version" != "$pkg_version" ]]; then
            echo "::error::tag version '$tag_version' != package version '$pkg_version'"
            exit 1
          fi
      - run: npm run release:preflight
      - name: Pack and checksum
        run: |
          set -o pipefail
          TARBALL=$(npm pack -w haechi-dashboard --silent)
          echo "TARBALL=$TARBALL" >> "$GITHUB_ENV"
          node scripts/release-checksums.mjs "$TARBALL" | tee SHA256SUMS
      # actions/attest-build-provenance v2.4.0
      - uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be
        with:
          subject-path: ${{ env.TARBALL }}
      - run: npm publish -w haechi-dashboard --provenance --access public
      - name: Upload release artifacts
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${{ github.event.release.tag_name }}" "$TARBALL" SHA256SUMS --clobber
